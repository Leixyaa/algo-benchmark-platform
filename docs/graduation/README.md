# 毕设资料目录

将毕设相关资料集中放在此目录，建议优先提供可复制文本（`.md` / `.txt`）。

建议文件清单：
- `开题报告.md`（或 `.txt`）
- `任务书.md`（或 `.txt`）
- `学校毕设要求.md`（或 `.txt`）
- `论文格式规范.md`（可选）

如果只有 PDF/DOCX，建议同时导出一份 `.md` 或 `.txt` 放在同目录，便于稳定提取内容。

